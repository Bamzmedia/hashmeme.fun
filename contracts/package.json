{
  "name": "hashmeme-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for HashMeme",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
